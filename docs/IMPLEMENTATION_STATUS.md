# Pulse 实现与验收状态

更新时间：2026-08-10  
当前结论：核心实现与 B 方向视觉工程完成；自动化工程门禁 GO；最终界面、完整工程验收及公开发布 NO-GO

## 已完成

- 删除 SwiftUI Hello World 模板和旧入口，不保留演示路径。
- 最低系统版本统一为 iOS / iPadOS 17.0。
- 建立版本化 SwiftData schema 和显式迁移计划。
- 建立单一主签到项目和 `habitID + logicalDay` 唯一记录键。
- 统一固定签到时区、逻辑日、跨日、连续天数和月历分类算法。
- 所有写入统一经 `SwiftDataCheckInRepository`，保存失败执行回滚。
- 今日签到、最近 7 天、当前连续、历史月历、最长连续和累计统计。
- 单条记录删除、全部数据清除和所有派生状态重算。
- 每日提醒权限、30 天滚动一次性通知和签到后取消当天提醒。
- 提醒时间以单一“午夜起分钟数”值存储，UI 与通知调度不读取设备当前日历。
- 签到时区、一周起始日和触觉反馈设置。
- 版本化 JSON 导出，以及校验完成后全量替换的 JSON 导入恢复。
- 中英文 String Catalog、深色模式、动态字体和基础 VoiceOver 标签。
- iPhone / iPad 自适应布局。
- 共享 Xcode scheme；仓库不再跟踪个人 `xcuserdata`。
- B“安静的每日仪式”作为 1.0 唯一视觉系统，颜色由 Asset Catalog 集中定义。
- 移除旧紫色渐变、毛玻璃、橙色火焰、多色统计卡和页面内颜色硬编码。
- B2“开放日环”AppIcon 已录用；单一遮罩确定性生成 Default / Dark / Tinted 资源。

## 自动化验证

验证环境：Xcode 26.4，iOS 18.6 iPhone 16 Pro 模拟器。

- Debug 模拟器构建：通过。
- Release 模拟器构建：通过。
- Release `iphoneos` 通用真机架构构建（不签名）：通过。
- Xcode 静态分析：通过，无源码分析告警。
- 单元、集成和 UI 测试：31 / 31 通过。
- Asset Catalog 编译并输出 iPhone / iPad AppIcon：通过。
- AppIcon 生成脚本重复执行 SHA-256 一致：通过。
- UI 测试覆盖：首次状态、签到、禁用重复签到、终止重启后持久化、历史统计显示。
- 日期覆盖：零点、非零日界线、跨月、跨年、闰年、夏令时和固定时区。
- 数据覆盖：幂等签到、删除、清空、导出往返、有效导入、无效导入不破坏现有数据。
- 设置覆盖：提醒时间边界、选择器往返和损坏设置拒绝启动。

## 模拟器视觉检查

- B 方向 iPhone 16 Pro 浅色、深色和无障碍超超超大字号已检查，核心流程可滚动完成。
- B 方向 iPad Pro 11 英寸浅色标准字号已检查。
- B2 AppIcon 已检查 1024 / 180 / 60 / 40 / 29 px，并在模拟器主屏检查系统裁切后的 60 pt 表现。
- 历史页在 UI 测试签到后截图中显示正确的当前、最长、累计和月历状态。
- 视觉检查发现并修复英文星期标题重复 ID 导致 T / S 缺失的问题；UI 测试现在断言 7 个标题完整呈现。

模拟器截图是工程布局证据，不等于最终视觉验收。

`iphoneos` 不签名构建只证明真机架构可编译，不证明签名、安装、硬件行为或后台通知通过。

## 当前未完成的发布门槛

### 最终界面视觉 NO-GO

- B 方向和 B2 AppIcon 已完成选择、实现和工程验证。
- 今日页、历史页和 iPad 自适应截图已形成，但重构后的完整界面尚未得到最终产品视觉确认。
- App Store 截图构图与商店素材仍未设计和验收。

### 真机 NO-GO

- 尚未在真实 iPhone 验证触觉、通知授权、通知到达、系统设置切换和长期后台调度。
- 尚未在真实 iPad 验证横竖屏与分屏。
- 尚未在真机验证 SwiftData 持久化、强制退出重启、删除和清空。

因此，按照测试计划中的完整工程 GO 标准，当前完整工程验收仍为 NO-GO；不能用自动化门禁结果替代真机证据。

### 数据策略已冻结

- 当前本地数据可通过 JSON 手动导入/导出恢复。
- CloudKit 和其他自动多设备同步明确不进入 1.0，不保留隐藏入口或半成品实现。

### 发布 NO-GO

- 尚未完成商店截图、隐私说明和支持页面。
- 尚未执行签名归档、TestFlight 安装和 App Store 校验。
- Bundle ID `cool.pulse` 和正式版本号仍需发布负责人确认。

## 下一步顺序

1. 对 B 方向最终界面完成产品视觉确认。
2. 在真实 iPhone / iPad 执行通知、触觉、旋转、分屏和持久化验收。
3. 完成隐私、商店截图、签名、归档和 TestFlight 验收。
