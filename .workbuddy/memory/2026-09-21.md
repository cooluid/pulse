# 2026-09-21

## 静野/晴昼主题重设计（Build 10 外观线）
- 诊断：quietField/sunlitDay 原挂在 PulseTodayPage 的 legacyComposition（换皮主题，无独立构图），这是审美差的根因。
- 重设计落地：
  - 静野→「一茎草」：新文件 pulse/Shared/PulseQuietFieldTodayPage.swift，streak=一株草的节点（Canvas 贝塞尔求值），签到=水墨笔触胶囊，星期轨=七片草叶（高度即状态）。
  - 晴昼→「蓝晒」：新文件 pulse/Shared/PulseSunlitTodayPage.swift，整屏普鲁士蓝（light #143A60 / dark #0C1F36），日期数字=曝光显影（透明度 0.30→1），签到=白色印相胶囊，星期轨=显影圆。
  - 令牌：brand-tokens.json 重写 quiet*/sunlit* 色板，删除 8 个死角色（quietBlue/Pink/Yellow/Chrome/GreenDeep、sunlitMap/MapDeep）+ 8 个孤儿 colorset；build_brand_assets.py 同步。
  - 清死代码：PulseDesignSystem 删约 40 个无人引用的常量（quietAmbient*/sunlitAmbient*/editorialAmbient* 等）、PulseQuietSpeechBubbleShape、PulseSunlitSurfaceFill、PulsePageFold、PulseSeedShape。
  - 消费方跟进：HistoryView/JournalNoteViews/EnhancementStoreView/PulseCalendarDayStyle 的 quiet/sunlit 分支改为衬线+墨格/蓝晒几何；主题描述文案更新（xcstrings，注意保持 Xcode 的 " : " JSON 格式）。
- 验证：xcodebuild build SUCCEEDED + 204 单元测试全过 + build_brand_assets --check 通过。
- 环境坑（重要）：本机 shell 里 swift 宏展开必挂（sandbox-exec: Operation not permitted → swift-plugin-server malformed response），最小复现 swiftc -typecheck @Observable。解法：OTHER_SWIFT_FLAGS='$(inherited) -disable-sandbox'。UI 测试未跑（重）。
