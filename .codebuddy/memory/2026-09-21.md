# 2026-09-21

## 界面主题问题的根因诊断（待用户选路线）

用户反馈：界面主题改了 N 版都不理想，AI 每轮只改样式不改布局，主题名约束发挥，产出全是细线条，没有付费欲望。

排查结论：

1. **根因是架构，不是美术。** `PulseVisualTheme`（pulse/Data/AppSettings.swift）在代码里唯一能力是提供 `PulseThemePalette` 的 9 个颜色槽。布局差异只能靠散落的 `if theme == .xxx` / `switch theme` 硬编码，分布在 `PulseTodayPage`、`PulseCheckInFace`、`PulsePrimaryNavigation`、`PulseAppHeader`、`PulseScreenBackground`、`SettingsView`、`EnhancementStoreView` 等 6+ 文件。改颜色是最低成本路径，所以每轮迭代必然只出"换漆"。
2. **名字不是根因，是放大器。** 主题名是视觉隐喻承诺（sunlitDay / moonTide），但布局层没有可调参数，隐喻只能用装饰兑现 → `PulseStampOutline`、`PulsePageFold`、`PulseTideTrace`、`PulseSeedShape`、棱镜刻度线即"垃圾线条"。改名史（Tidal Breath → Tide Archive → Sunlit Day，Fault Almanac 被移除）证明改名无效。
3. **付费结构反了。** 免费主题 `editorialJournal` 是设计最完整的一套；付费主题只是同一页加装饰。价格必须来自结构差异而非装饰密度。
4. **`PulseTodayPage` 的泛型注入（checkIn/journal/media）已经把"事实"原子化了，可复用。** 但它把"编排"也写死了，主题只能做 `padding(.top, theme == .moonTide ? 96 : 0)` 这类数字魔法微调。
5. 测试现状：`pulseTests/ThemeExperienceContractTests.swift` 只锁权益/免费主题归属，未锁几何与颜色（符合 AGENTS.md 边界）。`PulseDesignSystem.swift` 无测试锁定。

候选路线（未定）：

- A（主推）：编排契约化。事实层保持唯一实现，编排抽成协议，每主题一个实现文件；`switch theme` 消失，改为多态。新增/重画主题不动其他主题与事实层。
- B：参数化 Recipe 结构体。风险是参数化陷阱，加了参数仍是同一页，不建议做主方案。
- C：命名滞后。先做体验后命名；名字从视觉隐喻改为使用场景/人格。需与 A 同时做，单独做等于再次改名。

状态：等待用户选择路线，尚未改代码。

## 实施结果（路线 A + C，同日完成）

用户选择路线 A + C，要求风格"融合/流体"，明确不要平庸。

1. **编排契约**：`PulseTodayPage` 拆为「事实容器 `PulseTodayFacts` + 分派器 + `legacyComposition`」。新主题走 `PulseImmersionTodayPage` 独立文件，旧主题路径原样保留（零回归）。对外接口未变，`TodayView` / `PulseVisualThemeSpecimen` 无需改动。未来重做其他主题只需逐个搬出 legacy。
2. **新增主题 `immersion`（中文「沉浸」）**，名字描述体验而非视觉隐喻，避免重蹈"按名字画线条"。强制深色（与 moonTide 同策略），青绿 accent `#43E0B8`，底色 `#080B10`。
   - 结构差异：巨大日号（176pt black）右对齐、底部渐隐，作为页面唯一强音；签到面是全宽 104 高、圆角 38 的有机面（未签到 = accent 实色 + 辉光，已签到 = accentSoft 静默态）；周流用 Canvas 画高低起伏的连续带取代 7 个胶囊。全程无卡片、无 1px 装饰线。
3. 令牌：9 个 `immersion*` 颜色写入 `design/brand-tokens.json` 与 `build_brand_assets.py` 的 `COLOR_ASSETS`，生成 9 个 colorset。
4. 测试：`ThemeExperienceContractTests` 更新期望并新增 immersion 令牌断言；新增 UI 测试 `testImmersionThemeRendersSurfacePickerAndCheckIn`（切主题 + 截图）。
5. 验证：编译通过；203 个单元测试通过；既有主题两个关键 UI 测试通过。

顺手修复（会话开始前已存在，非本次引入）：
- `pulse/Localizable.xcstrings` 有空 key `""` 导致 String Catalog 解码失败
- 4 个 stale 死条目（`today.check_in`、`today.commitment.cue`、`today.navigation.pending`、`history.navigation.subtitle`）代码零引用，已删
- `settings.visual_theme.preview` 被误标 stale 但代码在用，改回 manual
- `.gitignore` 补 `/.build/`

**关键认知（勿忘）**：`PulseCheckInFace` 是 `accessibilityHidden`，签到按钮可达性由 `TodayView` 的 `PulseCombinedPressControl` overlay 提供 —— 视觉与可达性已解耦，重画签到控件视觉不会影响 UI 测试。UI 测试依赖的标识符：`today.checkin.button`、`today.week.rail`、`today.rhythm.status`、`today.commitment.name`、`today.hero.kicker`、`today.week.day.<x>`、`today.media.capture.button`。

截图：`.build/immersion-shots/`（构建产物，勿提交）。视觉是否通过由人判断。

## 收尾（同日）

- immersion 记事区加了 18pt 的 accent 短标记。放在 `PulseImmersionTodayPage` 的编排层（`journal.padding(.leading,12).overlay(alignment:.topLeading)`），**不改 `JournalNoteViews`**，其他主题零影响。先试过通高竖条，视觉上像未闭合边框，已改短。
- 用真实多天数据验证周流：把 `"immersion"` 加进 `testApprovedThemesAcrossRecordedDays` 的 themes 数组即可（该测试自带 3 天历史 + 遍历主题截图）。4 天积累下（细灰条 → 绿段 → 今天凸起）节奏带成立，设计意图达成。
- **已知环境噪音**：`pulse/Localizable.xcstrings` 会冒出空条目 `"" : {}`（在字符串表开头），使 `PulseWidgetProjectionTests` 两个测试解码失败。已删两次；第二次删除后跑测试未复发，判断是 Xcode 的 String Catalog 编辑器打开该文件时写回。若再遇到，删开头空条目即可，不是代码问题。
- 当前状态：203 单元测试全绿；immersion 的 UI 测试与既有主题流程测试均通过。

## 月汐重做 + 一处建议纠正

**纠正（重要）**：先前建议"付费主题减到 2-3 套"是错的。查证后：商店付费墙已公开点名四套（`store.capability.themes.title` = "四套额外界面主题"，`store.hero.promise` 与 `store.hero.tagline` 都列了主题名），随 1.1 (9) 发布；且 `AppSettings.init` 里 `PulseVisualTheme(rawValue:)` 解析失败会抛 `PulseAppError.invalidSettings`，导致 app 进 failed 状态。**所以数量必须保住，路线改为四套全部重做成结构不同。**

**月汐重做**（新增 `pulse/Shared/PulseMoonTideTodayPage.swift`，并在 `PulseTodayPage` 分派器接 `case .moonTide`）：从"日期 + 标题 + 居中按钮"改为**影像主导**——海面照片铺满上半屏、上下缘渐隐融入画布；日期/承诺名/签到浮在照片上；签到控件改为 `.ultraThinMaterial` 玻璃面（74 高、圆角 22）；周条改为大小不一的涟漪圆点。
`PulseCheckInFace` 的 moonTide 分支已重写（原 `PulseTideTrace` + 圆点形态弃用，资源仍在）。

两套新方向的区分：**沉浸 = 纯字体 + 实色面 + 连续带**；**月汐 = 影像 + 玻璃 + 涟漪点**。

剩余待重做：`sunlitDay`、`quietField`、`prismLedger`。`editorialJournal` 作为免费基线暂留 legacy。

## 滚动割裂修复（用户反馈）

**用户反馈**："上下滑动发现背景色和顶部那里割裂了"。截图确认两套都有同一病根：**主视觉是 ScrollView 内容的一部分，滚上去被硬切**（沉浸的日号被裁成平头；月汐的照片底边跑到屏幕中间形成三段式）。

**修法（改架构而非加渐变）**：新增 `pulse/Shared/PulseThemeBackdrop.swift`，把"不滚动的部分"独立出来。
- 沉浸：日期行 + 超大日号移到 backdrop（固定在 safe area 顶 + `PulseDesign.topBarHeight` + 4）
- 月汐：海面照片移到 `PulseFieldBackground` 的 moonTide 分支（全屏、固定），日期行移到 backdrop
- 内容侧改为 `Color.clear.frame(height:)` 占位（沉浸 243 / 26，月汐 158 / 24）
- `TodayView` 与 `PulseVisualThemeSpecimen` 都加了 backdrop
- 日期行保持可达（承载 `today.hero.kicker`），装饰性数字与照片 `accessibilityHidden`

**观感调整**：日号在内容重叠区退让更快（`accent.opacity(0.36)`，mask 在 0.44/0.80 处快速衰减），否则滚动后承诺名会与日号抢注意力。

**已知环境噪音复发**：`Localizable.xcstrings` 的空条目 `"" : {}` 又出现一次（第三次），再次删除后 203 单测全绿。确认与构建无关，是外部（Xcode String Catalog 编辑器）写入。

## 月汐去影像化（用户否掉照片方案）

**用户判断**："这个主题不要了吧，好丑。它靠背景图来设计界面主题了。" —— 判断正确，我上一轮把照片铺满全屏反而把这个错误做得更彻底。

**改动**：
- `PulseFieldBackground` 的 moonTide 分支：海面照片 → 居中 RadialGradient 光场（`moonAccent.opacity(0.10)` 内芯 + `moonSurface.opacity(0.92)` 外圈，中心 `(0.5, 0.20)`，半径 520）
- `PulseThemeBackdrop` 的 moonTide 日期行改为居中
- `PulseMoonTideTodayPage` 重写为**居中对称、低对比**：承诺名 28pt `.light` 字重、字距 0.4；连续天数 12pt 字距 1.6；签到控件收窄（左右各 56）+ 胶囊；周点 `HStack(spacing: 20)` 居中聚拢，只显示日号
- `PulseCheckInFace` 的 moonTide 分支：未签到 = accent 实色胶囊 + 深色字 + 辉光（唯一亮点）；已签到 = `accent.opacity(0.18)` + accent 字（安静下来）
- 删掉了 `moonGlass`（`.ultraThinMaterial` 玻璃）

**两套的区分**：沉浸 = 不对称（右对齐大日号）+ 全宽实色面 + 响亮；月汐 = 对称居中 + 窄胶囊 + 低对比安静 + 一片冷光。

**遗留矛盾（已告知用户）**：`moonTide` 这个名字（月夜水面）本身就在要求视觉去表现"水"；不用照片就只能画波浪，也就是用户最早否掉的装饰线条。建议改显示名（`rawValue` 不能动），但要同步改商店文案。

## 测试容错修复（重要）

`pulseTests/PulseWidgetProjectionTests.swift` 的 `WidgetStringCatalog` 原来 `Entry.localizations` 是必填的，于是 Xcode String Catalog 编辑器留下的空条目 `""` 会让整个解码抛错、两个测试全挂。已改：
- `Entry.localizations` 用 `decodeIfPresent` + 默认 `[:]`
- `WidgetStringCatalog.strings` 自定义 `init(from:)` **过滤掉空 key**（运行时代码不可能引用空 key，它不承载任何文案）

验证方式：故意把空条目加回去跑测试 → 203 全绿 → 再删掉。以后不必再反复手删这个噪音。

## 删除月汐主题（用户决定）

用户："月汐这个主题直接删掉吧，对于已经切换成这个的，默认显示成免费的，让用户自己去选择便是。"

**最关键的一步不是删代码，是改加载语义**：`AppSettings.init` 原来 `PulseVisualTheme(rawValue:)` 失败就 `throw`，老用户存着 `"moonTide"` 会直接掉进 `RootView` 的 failed 页面。已改成回退 `PulseVisualThemeAccessPolicy.freeTheme`（`pulse/Data/AppSettings.swift` 的 `loadedVisualTheme`），新增测试 `testRetiredThemeFallsBackToTheFreeThemeInsteadOfFailingTheLoad` 守住。**这条改动现在让"删主题 case"成为安全操作。**

清理清单（全部完成）：
- 删 `pulse/Shared/PulseMoonTideTodayPage.swift`
- 删 `PulseMoonSeascapeView` 定义 + `PulseMoonSeascape`/`PulseTideTick`/`PulseTideTrace` 三个 imageset
- `PulseTodayPage`（分派器、legacy 条件、heading/titleFont/artwork 分支）、`PulseThemeBackdrop`、`PulseFieldBackground`、`PulseDesignSystem`（palette/appSuccess/AppHeader 条件）、`PulseCalendarDayStyle`、`HistoryView`(7 处)、`JournalNoteViews`(6 处)、`EnhancementStoreView`(3 处)、`PulseThemeAppearance`
- xcstrings：删 `settings.visual_theme.moon_tide` 两条
- UI 测试：5 处主题数组/断言改用 `sunlitDay`

**商店承诺没破**：`store.capability.themes.detail` 与 `store.hero.promise` 里的「月汐」换成「沉浸」，`title`/`tagline` 的"四套"依然成立（immersion 顶替）。**没有减少数量，所以没有违反已公开承诺。**

测试：`ReminderTimeTests` 里 `testInvalidPersistedThemeOrLanguageFailsInitialization` 原来锁定"无效 visualTheme 必须抛错"，已改名 `...AppearanceOrLanguage...` 并去掉该段（行为有意变更）；`ThemeExperienceContractTests` 的 enhancementThemes 期望减去 moonTide。

结果：204 单测全绿；主题选择器只剩 4 套（纸页手记/静野/晴昼/棱镜刻度）+ 沉浸。
